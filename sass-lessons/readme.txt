examples of sass

sass install:
1. application prepros -> https://prepros.io/ + netframework
2. gulp
3. https://rubyinstaller.org ->
	gem install sass ->
	sass -v
		-> sass --watch style.sass:style.css (to current folder)
		-> sass --watch style.scss:style.css (to current folder, syntax as in css)
